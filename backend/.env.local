# Development Configuration (Local Storage)
PHOTO_STORAGE_TYPE=local
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dental_attendance
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
BACKEND_BASE_URL=http://localhost:8000
LOG_LEVEL=INFO

# Logging throttle control (in milliseconds)
# Lower values = more frequent logs, higher values = less frequent logs
# Examples:
#   100  = Log similar messages at most once per 0.1 seconds (very fast)
#   500  = Log similar messages at most once per 0.5 seconds (fast)
#   1000 = Log similar messages at most once per 1 second (default)
#   2000 = Log similar messages at most once per 2 seconds (slower)
LOG_THROTTLE_MS=100
